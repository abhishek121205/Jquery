$("document").ready(function(){
   $("#img").resizable({
    // animate: true,
    // containment: "parent",
    maxHeight: 400,
    minHeight: 100,
    ghost: true,
    // aspectRatio: true
   })
})